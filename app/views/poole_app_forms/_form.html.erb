<%= bootstrap_form_for(@poole_app_form) do |f| %>
    <% if @poole_app_form.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@poole_app_form.errors.count, "error") %> prohibited this poole_app_form from being
            saved:</h2>

          <ul>
            <% @poole_app_form.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <%= f.text_field :title %>

    <fieldset class="row">
      <%= f.text_field :api_secret, wrapper_class: 'col-md-6' %>
      <%= f.text_field :api_key, wrapper_class: 'col-md-6'  %>
    </fieldset>

    <div class="actions clearfix">
      <%= f.submit %>
    </div>
<% end %>
